local set = vim.cmd

-- sets the width of indentation to 4 chars
set 'setlocal shiftwidth=4'
-- sets the visual width of a tabulation to 4 chars
set 'setlocal tabstop=4'
-- makes it so that spaces are used instead of tabulations
set 'setlocal expandtab'
